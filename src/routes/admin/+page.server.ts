// import type { PageServerLoad } from "./$types";

// export const load = (async ({ locals }) => {
//   const uid = locals.uid;

//   try {
//     await fetch(`/api/vapidPubKey`);
//   } catch (e) {}
//   return { success: true };
// }) satisfies PageServerLoad;
